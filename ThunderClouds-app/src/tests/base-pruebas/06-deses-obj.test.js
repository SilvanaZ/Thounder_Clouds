import {usContext} from '../../base-pruebas/06-deses-obj';


describe('Pruebas en 06-deses-obj', () => {
    test('usContext deberia retornar un array', (clave, nombre, edad, rango) => {
        const data = {
        nombreClave: clave,
            anios: edad,
                latlng: {
            lat: 14.1232,
                lng: -12.3232
            }
     });
 });
